[project]
name = "tw-vh-bot"
version = "0.1.0"
description = "Telegram-бот для работы с виртуальным хостингом Timeweb через публичный API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
